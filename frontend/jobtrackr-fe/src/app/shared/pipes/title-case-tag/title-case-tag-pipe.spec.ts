import { TitleCaseTagPipe } from './title-case-tag-pipe';

describe('TitleCaseTagPipe', () => {
  it('create an instance', () => {
    const pipe = new TitleCaseTagPipe();
    expect(pipe).toBeTruthy();
  });
});
